# PyTorch with CUDA support
# For CUDA 12.x, install PyTorch separately:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0

# Core dependencies (required)
numpy>=1.24.0
scikit-learn>=1.2.0
matplotlib>=3.7.0
tqdm>=4.65.0
Pillow>=9.5.0  # Required for image processing in metrics.py

# Optional dependencies
scipy>=1.10.0  # Used for some advanced computations
torchaudio>=2.0.0  # Only if processing audio
seaborn>=0.12.0  # Enhanced visualizations

# Development/Optional
# jupyter>=1.0.0
# ipykernel>=6.22.0
